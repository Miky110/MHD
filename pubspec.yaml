name: mhd_mikylov
description: Palubní a cestující aplikace fiktivního dopravního systému MHD Mikylov.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  archive: ^3.6.1
  csv: ^6.0.0
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.3
  geolocator: ^14.0.2
  http: ^1.2.2
  just_audio: ^0.10.5
  path: ^1.9.1
  sqflite: ^2.4.2
  supabase_flutter: ^2.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
